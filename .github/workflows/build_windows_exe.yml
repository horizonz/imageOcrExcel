name: build-windows-exe

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller rapidocr-onnxruntime pillow pandas tqdm numpy opencv-python openpyxl
      - name: Build EXE
        run: |
          pyinstaller -F -w run_pipeline.py --collect-data rapidocr_onnxruntime
      - name: Prepare release folder
        run: |
          mkdir release
          mkdir release\run_pipeline
          copy dist\run_pipeline.exe release\run_pipeline\
          mkdir release\run_pipeline\images
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-windows
          path: release/run_pipeline
